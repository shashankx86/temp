name: ci # 
on:
  push:
    branches: # 
      - master
      - main

jobs:

  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          touch CANME
          echo "gg" > CNAME
